[pytest]
pythonpath = .
testpaths = tests
markers =
    gpu: requires a CUDA device (engine.py hardcodes cuda autocast)
    slow: slower integration-style test (full-size model / multi-sample dataset)
